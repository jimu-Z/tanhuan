<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.conversation.mapper.ExportTaskMapper">

    <resultMap type="ExportTask" id="ExportTaskResult">
        <id     property="taskId"       column="task_id"       />
        <result property="taskName"     column="task_name"     />
        <result property="exportType"   column="export_type"   />
        <result property="exportParams" column="export_params" />
        <result property="fileName"     column="file_name"     />
        <result property="filePath"     column="file_path"     />
        <result property="fileSize"     column="file_size"     />
        <result property="status"       column="status"        />
        <result property="errorMsg"     column="error_msg"     />
        <result property="recordCount"  column="record_count"  />
        <result property="createBy"     column="create_by"     />
        <result property="createTime"   column="create_time"   />
        <result property="finishTime"   column="finish_time"   />
    </resultMap>

    <sql id="selectExportTaskVo">
        select task_id, task_name, export_type, export_params, file_name, file_path,
               file_size, status, error_msg, record_count, create_by, create_time, finish_time
        from biz_export_task
    </sql>

    <select id="selectExportTaskList" parameterType="ExportTask" resultMap="ExportTaskResult">
        <include refid="selectExportTaskVo"/>
        <where>
            <if test="taskName != null and taskName != ''">
                AND task_name like concat('%', #{taskName}, '%')
            </if>
            <if test="exportType != null and exportType != ''">
                AND export_type = #{exportType}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="createBy != null and createBy != ''">
                AND create_by = #{createBy}
            </if>
        </where>
        order by create_time desc
    </select>

    <select id="selectExportTaskById" parameterType="Long" resultMap="ExportTaskResult">
        <include refid="selectExportTaskVo"/>
        where task_id = #{taskId}
    </select>

    <select id="selectPendingTasks" resultMap="ExportTaskResult">
        <include refid="selectExportTaskVo"/>
        where status = '0'
        order by create_time asc
    </select>

    <insert id="insertExportTask" parameterType="ExportTask" useGeneratedKeys="true" keyProperty="taskId">
        insert into biz_export_task(
            <if test="taskName != null and taskName != ''">task_name,</if>
            <if test="exportType != null and exportType != ''">export_type,</if>
            <if test="exportParams != null and exportParams != ''">export_params,</if>
            <if test="fileName != null and fileName != ''">file_name,</if>
            <if test="filePath != null and filePath != ''">file_path,</if>
            <if test="fileSize != null">file_size,</if>
            <if test="status != null and status != ''">status,</if>
            <if test="errorMsg != null and errorMsg != ''">error_msg,</if>
            <if test="recordCount != null">record_count,</if>
            <if test="createBy != null and createBy != ''">create_by,</if>
            create_time
        )values(
            <if test="taskName != null and taskName != ''">#{taskName},</if>
            <if test="exportType != null and exportType != ''">#{exportType},</if>
            <if test="exportParams != null and exportParams != ''">#{exportParams},</if>
            <if test="fileName != null and fileName != ''">#{fileName},</if>
            <if test="filePath != null and filePath != ''">#{filePath},</if>
            <if test="fileSize != null">#{fileSize},</if>
            <if test="status != null and status != ''">#{status},</if>
            <if test="errorMsg != null and errorMsg != ''">#{errorMsg},</if>
            <if test="recordCount != null">#{recordCount},</if>
            <if test="createBy != null and createBy != ''">#{createBy},</if>
            sysdate()
        )
    </insert>

    <update id="updateExportTask" parameterType="ExportTask">
        update biz_export_task
        <set>
            <if test="taskName != null and taskName != ''">task_name = #{taskName},</if>
            <if test="exportType != null and exportType != ''">export_type = #{exportType},</if>
            <if test="exportParams != null">export_params = #{exportParams},</if>
            <if test="fileName != null">file_name = #{fileName},</if>
            <if test="filePath != null">file_path = #{filePath},</if>
            <if test="fileSize != null">file_size = #{fileSize},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="errorMsg != null">error_msg = #{errorMsg},</if>
            <if test="recordCount != null">record_count = #{recordCount},</if>
            <if test="finishTime != null">finish_time = #{finishTime},</if>
        </set>
        where task_id = #{taskId}
    </update>

    <delete id="deleteExportTaskById" parameterType="Long">
        delete from biz_export_task where task_id = #{taskId}
    </delete>

    <delete id="deleteExportTaskByIds" parameterType="Long">
        delete from biz_export_task where task_id in
        <foreach collection="array" item="taskId" open="(" separator="," close=")">
            #{taskId}
        </foreach>
    </delete>

</mapper>