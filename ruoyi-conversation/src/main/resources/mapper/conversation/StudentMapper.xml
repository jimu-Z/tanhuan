<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.conversation.mapper.StudentMapper">

    <resultMap type="Student" id="StudentResult">
        <id     property="studentId"   column="student_id"   />
        <result property="studentNo"   column="student_no"   />
        <result property="studentName" column="student_name" />
        <result property="gender"      column="gender"       />
        <result property="collegeName" column="college_name" />
        <result property="majorName"   column="major_name"   />
        <result property="className"   column="class_name"   />
        <result property="deptId"      column="dept_id"      />
        <result property="majorId"     column="major_id"     />
        <result property="classId"     column="class_id"     />
        <result property="phone"       column="phone"        />
        <result property="email"       column="email"        />
        <result property="dormitory"   column="dormitory"    />
        <result property="status"      column="status"       />
        <result property="delFlag"     column="del_flag"     />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"  column="create_time"  />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"  column="update_time"  />
        <result property="remark"      column="remark"       />
    </resultMap>

    <sql id="selectStudentVo">
        select s.student_id, s.student_no, s.student_name, s.gender, s.college_name,
               s.major_name, s.class_name, s.dept_id, s.major_id, s.class_id,
               s.phone, s.email, s.dormitory, s.status, s.del_flag,
               s.create_by, s.create_time, s.update_by, s.update_time, s.remark
        from biz_student s
    </sql>

    <select id="selectStudentList" parameterType="Student" resultMap="StudentResult">
        select s.student_id, s.student_no, s.student_name, s.gender, s.college_name,
               s.major_name, s.class_name, s.dept_id, s.major_id, s.class_id,
               s.phone, s.email, s.dormitory, s.status, s.del_flag,
               s.create_by, s.create_time, s.update_by, s.update_time, s.remark,
               m.major_name as major_name_text, c.class_name as class_name_text
        from biz_student s
        left join biz_major m on s.major_id = m.major_id
        left join biz_class c on s.class_id = c.class_id
        <where>
            <if test="studentNo != null and studentNo != ''">
                AND s.student_no like concat('%', #{studentNo}, '%')
            </if>
            <if test="studentName != null and studentName != ''">
                AND s.student_name like concat('%', #{studentName}, '%')
            </if>
            <if test="gender != null and gender != ''">
                AND s.gender = #{gender}
            </if>
            <if test="deptId != null">
                AND s.dept_id = #{deptId}
            </if>
            <if test="majorId != null">
                AND s.major_id = #{majorId}
            </if>
            <if test="classId != null">
                AND s.class_id = #{classId}
            </if>
            <if test="status != null and status != ''">
                AND s.status = #{status}
            </if>
            <if test="phone != null and phone != ''">
                AND s.phone like concat('%', #{phone}, '%')
            </if>
            ${params.dataScope}
        </where>
    </select>

    <select id="selectStudentById" parameterType="Long" resultMap="StudentResult">
        <include refid="selectStudentVo"/>
        where s.student_id = #{studentId}
    </select>

    <select id="selectStudentByStudentNo" parameterType="String" resultMap="StudentResult">
        <include refid="selectStudentVo"/>
        where s.student_no = #{studentNo}
    </select>

    <select id="checkStudentNoUnique" parameterType="String" resultMap="StudentResult">
        <include refid="selectStudentVo"/>
        where s.student_no = #{studentNo} limit 1
    </select>

    <select id="selectStudentByDeptId" parameterType="Long" resultMap="StudentResult">
        <include refid="selectStudentVo"/>
        where s.dept_id = #{deptId}
    </select>

    <insert id="insertStudent" parameterType="Student" useGeneratedKeys="true" keyProperty="studentId">
        insert into biz_student(
            <if test="studentNo != null and studentNo != ''">student_no,</if>
            <if test="studentName != null and studentName != ''">student_name,</if>
            <if test="gender != null and gender != ''">gender,</if>
            <if test="collegeName != null and collegeName != ''">college_name,</if>
            <if test="majorName != null and majorName != ''">major_name,</if>
            <if test="className != null and className != ''">class_name,</if>
            <if test="deptId != null">dept_id,</if>
            <if test="majorId != null">major_id,</if>
            <if test="classId != null">class_id,</if>
            <if test="phone != null and phone != ''">phone,</if>
            <if test="email != null and email != ''">email,</if>
            <if test="dormitory != null and dormitory != ''">dormitory,</if>
            <if test="status != null and status != ''">status,</if>
            <if test="createBy != null and createBy != ''">create_by,</if>
            <if test="remark != null and remark != ''">remark,</if>
            create_time
        )values(
            <if test="studentNo != null and studentNo != ''">#{studentNo},</if>
            <if test="studentName != null and studentName != ''">#{studentName},</if>
            <if test="gender != null and gender != ''">#{gender},</if>
            <if test="collegeName != null and collegeName != ''">#{collegeName},</if>
            <if test="majorName != null and majorName != ''">#{majorName},</if>
            <if test="className != null and className != ''">#{className},</if>
            <if test="deptId != null">#{deptId},</if>
            <if test="majorId != null">#{majorId},</if>
            <if test="classId != null">#{classId},</if>
            <if test="phone != null and phone != ''">#{phone},</if>
            <if test="email != null and email != ''">#{email},</if>
            <if test="dormitory != null and dormitory != ''">#{dormitory},</if>
            <if test="status != null and status != ''">#{status},</if>
            <if test="createBy != null and createBy != ''">#{createBy},</if>
            <if test="remark != null and remark != ''">#{remark},</if>
            sysdate()
        )
    </insert>

    <update id="updateStudent" parameterType="Student">
        update biz_student
        <set>
            <if test="studentNo != null and studentNo != ''">student_no = #{studentNo},</if>
            <if test="studentName != null and studentName != ''">student_name = #{studentName},</if>
            <if test="gender != null and gender != ''">gender = #{gender},</if>
            <if test="collegeName != null">college_name = #{collegeName},</if>
            <if test="majorName != null">major_name = #{majorName},</if>
            <if test="className != null">class_name = #{className},</if>
            <if test="deptId != null">dept_id = #{deptId},</if>
            <if test="majorId != null">major_id = #{majorId},</if>
            <if test="classId != null">class_id = #{classId},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="dormitory != null">dormitory = #{dormitory},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            <if test="remark != null">remark = #{remark},</if>
            update_time = sysdate()
        </set>
        where student_id = #{studentId}
    </update>

    <delete id="deleteStudentById" parameterType="Long">
        delete from biz_student where student_id = #{studentId}
    </delete>

    <delete id="deleteStudentByIds" parameterType="Long">
        delete from biz_student where student_id in
        <foreach collection="array" item="studentId" open="(" separator="," close=")">
            #{studentId}
        </foreach>
    </delete>

    <select id="countTotalStudents" resultType="Long">
        select coalesce(count(1), 0) from biz_student
    </select>

</mapper>